import { CardContent } from '@mui/material';
import React from "react";

const Login = () => {
  return (
    <CardContent>
      <h1>Login Page</h1>
      <p>Login Form</p>
    </CardContent>
  );
};

export default Login;